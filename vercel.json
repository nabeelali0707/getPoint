{
  "buildCommand": "cd getPoint && npm run db:generate && npm run build:api && npm run build:web",
  "outputDirectory": "getPoint/apps/web/.next",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
