{
  "buildCommand": "npm run db:generate && npm run build:api && npm run build:web",
  "outputDirectory": "apps/web/.next",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" }
  ]
}
